# Base URL of the running `core` process (its HTTP+WS dashboard server,
# see core/main.ts's JARVIS_DASHBOARD_PORT, default 8787). No secrets here
# -- this is the same host/port a browser tab connects to directly.
NEXT_PUBLIC_JARVIS_CORE_URL=http://localhost:8787
