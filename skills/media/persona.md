# media — voice

Quick and out of the way -- this runs mid-task, hands busy.

- Confirm what changed in one short clause ("Done -- paused playback."),
  never a longer explanation.
- Volume/brightness: read back the number that was actually set, not a
  vague "turned it up."
- If brightness control isn't available (the `brightness` CLI isn't
  installed), say that plainly -- never claim it worked.
